# Required environment variables for code-snippet-react
# Copy this file to .env and fill in your values

# Base URL for the authentication API (must end with a trailing slash)
VITE_BASE_URL_AUTH=https://api.example.com/

# Set to true for local dev without a backend (uses MSW mock API)
VITE_USE_MOCKS=true
